<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shuyuansys.dao.ArticleMapper">
    <resultMap id="BaseResultMap" type="com.shuyuansys.po.Article">
        <!--
          WARNING - @mbggenerated
            字段
        -->

 <result property="id" column="id" />
 <result property="cid" column="cid" />
 <result property="title" column="title" />
 <result property="picurl" column="picurl" />
 <result property="memo" column="memo" />
 <result property="uptime" column="uptime" />


    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated

        -->
        id,cid,title,picurl,memo,uptime
    </sql>
    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        <!--varticle
          WARNING - @mbggenerated
        查询，有视图就用视图
        -->
        select
        *
        from varticle
        where id = #{id,jdbcType=INTEGER}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        <!--
         删除
        -->
        delete from article
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <insert id="insert" parameterType="com.shuyuansys.po.Article">
        <!--
            添加全字段
        -->
        insert into article (id,cid,title,picurl,memo,uptime)
        values (#{id ,jdbcType=INTEGER},#{cid ,jdbcType=INTEGER},#{title ,jdbcType=VARCHAR},#{picurl ,jdbcType=LONGVARCHAR},#{memo ,jdbcType=LONGVARCHAR},#{uptime ,jdbcType=VARCHAR})
    </insert>
    <insert id="insertSelective" parameterType="com.shuyuansys.po.Article">
        <!--
          WARNING - @mbggenerated
             添加不为空的字段
        -->
        insert into article
        <trim prefix="(" suffix=")" suffixOverrides=",">
             <if test="id != null">
id,
        </if>
 <if test="cid != null">
cid,
        </if>
 <if test="title != null">
title,
        </if>
 <if test="picurl != null">
picurl,
        </if>
 <if test="memo != null">
memo,
        </if>
 <if test="uptime != null">
uptime,
        </if>

        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
             <if test="id != null">
#{id ,jdbcType=INTEGER},
 </if>
 <if test="cid != null">
#{cid ,jdbcType=INTEGER},
 </if>
 <if test="title != null">
#{title ,jdbcType=VARCHAR},
 </if>
 <if test="picurl != null">
#{picurl ,jdbcType=LONGVARCHAR},
 </if>
 <if test="memo != null">
#{memo ,jdbcType=LONGVARCHAR},
 </if>
 <if test="uptime != null">
#{uptime ,jdbcType=VARCHAR},
 </if>

        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.shuyuansys.po.Article">
        <!--
        修改不为空的字段
        -->
        update article
        <set>
             <if test="cid != null">
cid = #{cid ,jdbcType=INTEGER},
 </if>
 <if test="title != null">
title = #{title ,jdbcType=VARCHAR},
 </if>
 <if test="picurl != null">
picurl = #{picurl ,jdbcType=LONGVARCHAR},
 </if>
 <if test="memo != null">
memo = #{memo ,jdbcType=LONGVARCHAR},
 </if>
 <if test="uptime != null">
uptime = #{uptime ,jdbcType=VARCHAR},
 </if>


        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
    <update id="updateByPrimaryKey" parameterType="com.shuyuansys.po.Article">
        <!--
         修改全部字段
        -->
        update article
        set

      cid = #{cid ,jdbcType=INTEGER},
title = #{title ,jdbcType=VARCHAR},
picurl = #{picurl ,jdbcType=LONGVARCHAR},
memo = #{memo ,jdbcType=LONGVARCHAR},
uptime = #{uptime ,jdbcType=VARCHAR}

        where id = #{id,jdbcType=INTEGER}
    </update>
    <select id="queryArticleInfoAll" parameterType="Map" resultType="com.shuyuansys.po.Article">
        SELECT * from varticle
        <where>
            <if test="whstr != null and whstr!=''">
                ${whstr}
            </if>
        </where>
        order by  ${orderby}
    </select>


    <select id="topArticleNumList"  parameterType="Map"  resultType="com.shuyuansys.po.Article">
        SELECT * from varticle
        <where>
            <if test="whstr != null and whstr!=''">
                ${whstr}
            </if>
        </where>

        order by  ${orderby}

        <if test=" limit!=null and limit>0">
            limit ${limit}
        </if>

    </select>







    <select id="topArticleNumListInTable"  parameterType="Map"  resultType="com.shuyuansys.po.Article">
        SELECT * from article
        <where>
            <if test="whstr != null and whstr!=''">
                ${whstr}
            </if>
        </where>

        order by  ${orderby}

        <if test=" limit!=null and limit>0">
            limit ${limit}
        </if>

    </select>




</mapper>