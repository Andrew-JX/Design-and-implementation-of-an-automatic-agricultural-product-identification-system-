<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shuyuansys.dao.WenfeiMapper">
    <resultMap id="BaseResultMap" type="com.shuyuansys.po.Wenfei">
        <!--
          WARNING - @mbggenerated
            字段
        -->

         <result property="id" column="id" />
 <result property="fenleiname" column="fenleiname" />


    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated

        -->
        id,fenleiname
    </sql>
    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        <!--wenfei
          WARNING - @mbggenerated
        查询，有视图就用视图
        -->
        select
        *
        from wenfei
        where id = #{id,jdbcType=INTEGER}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        <!--
         删除
        -->
        delete from wenfei
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <insert id="insert" parameterType="com.shuyuansys.po.Wenfei">
        <!--
            添加全字段
        -->
        insert into wenfei (id,fenleiname)
        values (#{id ,jdbcType=INTEGER},#{fenleiname ,jdbcType=VARCHAR})
    </insert>
    <insert id="insertSelective" parameterType="com.shuyuansys.po.Wenfei">
        <!--
          WARNING - @mbggenerated
             添加不为空的字段
        -->
        insert into wenfei
        <trim prefix="(" suffix=")" suffixOverrides=",">
             <if test="id != null">
id,
        </if>
 <if test="fenleiname != null">
fenleiname,
        </if>

        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
             <if test="id != null">
#{id ,jdbcType=INTEGER},
 </if>
 <if test="fenleiname != null">
#{fenleiname ,jdbcType=VARCHAR},
 </if>

        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.shuyuansys.po.Wenfei">
        <!--
        修改不为空的字段
        -->
        update wenfei
        <set>
             <if test="fenleiname != null">
fenleiname = #{fenleiname ,jdbcType=VARCHAR},
 </if>


        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
    <update id="updateByPrimaryKey" parameterType="com.shuyuansys.po.Wenfei">
        <!--
         修改全部字段
        -->
        update wenfei
        set

      fenleiname = #{fenleiname ,jdbcType=VARCHAR}

        where id = #{id,jdbcType=INTEGER}
    </update>
    <select id="queryWenfeiInfoAll" parameterType="Map" resultType="com.shuyuansys.po.Wenfei">
        SELECT * from wenfei
        <where>
            <if test="whstr != null and whstr!=''">
                ${whstr}
            </if>
        </where>
        order by  ${orderby}
    </select>


    <select id="topWenfeiNumList"  parameterType="Map"  resultType="com.shuyuansys.po.Wenfei">
        SELECT * from wenfei
        <where>
            <if test="whstr != null and whstr!=''">
                ${whstr}
            </if>
        </where>

        order by  ${orderby}

        <if test=" limit!=null and limit>0">
            limit ${limit}
        </if>

    </select>







    <select id="topWenfeiNumListInTable"  parameterType="Map"  resultType="com.shuyuansys.po.Wenfei">
        SELECT * from wenfei
        <where>
            <if test="whstr != null and whstr!=''">
                ${whstr}
            </if>
        </where>

        order by  ${orderby}

        <if test=" limit!=null and limit>0">
            limit ${limit}
        </if>

    </select>




</mapper>